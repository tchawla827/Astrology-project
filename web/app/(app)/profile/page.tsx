import { PlaceholderPage } from "@/components/app/PlaceholderPage";

export default function ProfilePage() {
  return <PlaceholderPage description="Profile settings land in Phase 13. Birth intake is already active in onboarding." title="Profile" />;
}
