import { RouteLoadingState } from "@/components/common/LoadingState";

export default function OnboardingLoading() {
  return <RouteLoadingState description="Preparing the next onboarding step..." title="Loading setup" />;
}
